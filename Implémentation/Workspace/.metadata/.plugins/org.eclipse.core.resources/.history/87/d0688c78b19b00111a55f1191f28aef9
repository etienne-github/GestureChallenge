package PlayerInterface;

import org.mt4j.util.MTColor;
import org.mt4j.util.math.Vector3D;

import GestureChallengeScene.GestureChallengeScene;

public class PlayerInterface {

	MTColor myColor;
	int myNumber;
	GestureChallengeScene myGCS;
	PlayerGoal myPG;
	
	public PlayerInterface(MTColor color, int playerNumber,GestureChallengeScene gCS){
		
		//init
		myColor=color;
		myNumber=playerNumber;
		myGCS = gCS;
		
		//creation
		myPG=new PlayerGoal(myGCS.getMTApplication(), new Vector3D(0,0), myGCS.getWorld(), myGCS.getScale(), myColor);
		
	}
}
